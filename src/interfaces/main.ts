export interface IDataSource {
    country_name: string,
    year: string,
    crop_name: string,
    crop_production: number | string,
    yield_of_crops: number | string,
    area_under_cultivation: number | string,
}